# 端口与监听地址
PORT=3000
HOST=0.0.0.0

# 对外站点地址（影响 sitemap 与 https 时的 Cookie Secure 标记）
SITE_URL=http://localhost:3000

# 管理员用户名白名单（登录/注册为这些用户时自动授予 admin 角色），英文逗号分隔
ADMIN_USERNAMES=admin

# 测试用临时库路径（db 单测会自动使用，无需手动设置）
# BLOG_DB_FILE=/tmp/blog-test.sqlite
